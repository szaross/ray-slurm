The task is to create a lab instruction for computer science students.

The task will be to compare hiperparameter tuning on ray cluster CPU vs GPU.
The cluster will be run on Athena (SLURM). 
The main thing for students to do will be to configure ray on multiple nodes on athenna. They don't need to write their own hiperparameter tuning

Athena docs: https://docs.hpc.cyfronet.pl/supercomputers/athena/