# Deprecated: Ares CPU jobs

CPU hyperparameter tuning now runs on **Athena** with `--gpus-per-trial 0` (see [`../athena/ray_tune_cpu.sbatch`](../athena/ray_tune_cpu.sbatch) and [docs/LAB.md](../../docs/LAB.md)).

These scripts are kept for reference only.
